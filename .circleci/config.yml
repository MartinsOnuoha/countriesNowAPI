version: 2.1
jobs:
  build:
    docker:
      - image: circleci/node:10-browsers
    steps:
      - checkout
      - run:
          name: Get branch code
          command: |
            echo 'Branch code fetched'
            echo 'This is the delivery pipeline'
      - run:
          name: List code directory
          command: |
            ls -la
      - run:
          name: 'Install packages'
          command: |
            npm install
